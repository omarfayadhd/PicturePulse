<section>
      <div class="container mt-5">
        <div class="row">
          {{#each products}}
 <div class="col-md-4">
          <div class="card" style="width: 20rem;">
  <img class="card-img-top" src="{{this.Image}}" alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">{{this.name}}</h5>
    <p class="para">"{{this.genere}}"</p>
    <a href="admin/view-products" class="btn btn-primary">MORE</a>
  </div>
</div>
</div>
  {{/each}}
  </div>
  </div>
  </div>
    </section>
    
z